# School Website (scaffold)

Open `index.html` in a browser to view the site locally.

Next steps:
- Replace placeholder text with your school's actual name, address, contact details.
- Provide real photos by replacing the image URLs in `index.html` or add them under an `assets/` folder.
- If you'd like, I can add an admin UI or turn this into a simple CMS-backed site.
